package courses.concordia.exception;

/**
 * Enum defining the types of entities within the application.
 * Used for identifying the specific type of entity related to an operation or an exception.
 */
public enum EntityType {
    USER,
    COURSE,
    INTERACTION,
    NOTIFICATION,
    REVIEW,
    SUBSCRIPTION,
    TOKEN
}
